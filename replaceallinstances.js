// JavaScript Program to Replace all Instances of a Character in a String

const str = "Hello Javascript";
const splitString = str.split('a');
const result = splitString.join('A');
console.log(result);